import css from './NavBar.module.css'


export const NavBar = () => {
    return (<>
        <div className={css.container} >
        <h1 className={css.title}>Choose a card</h1>
        </div>
        </>
    )
}

